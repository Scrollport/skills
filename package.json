{
  "name": "@scrollport/skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-exports.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
