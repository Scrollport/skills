{
  "name": "@scrollport/skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/sync-shared-references.mjs --check && node scripts/build-exports.mjs",
    "sync": "node scripts/sync-shared-references.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
